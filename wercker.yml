box: wercker/default
build:
  steps:
    - arjen/hugo-build@1.1.0:
        version: 0.14
        theme: casper
        flags: --buildDrafts=true
deploy:
  steps:
    - duleorlovic/ftp-deploy@1.0.1:
        destination: ftp://danvantran.com/danvantran/
        username: $USERNAME
        password: $PASSWORD