box: debian
build:
  steps:
    - arjen/hugo-build@1.14.1:
        version: 0.18
        theme: casper
        flags: --buildDrafts=true
deploy:
  steps:
    - bugscal/new-ftp-deploy@0.1.0
        destination: ftp://danvantran.com
        username: $USERNAME
        password: $PASSWORD